/**
 * Contains representations of matrices useful in 3D graphics (i.e 3x3 and 4x4 matrices). The representations are
 * designed as a low level wrappers for the matrixes for exchanging matrices with other libraries (e.g. OpenGL) and for
 * accumulating the results of multiple calculations. The classes therefore don't provide easy access to the underlying
 * matrix values.
 */
package com.sportdataconnect.geometry.matrix;
